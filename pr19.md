Title: Day 12: Filtered stats PDF v1.1 (KPIs + table + chart) + export action + tests

**Summary**  
Implements PDF report v1.1 for the active vehicle using the current StatsFilter + StatsMetric. The PDF includes KPIs (Total Cost, Distance, €/km, L/100km), a lightweight bar chart (Cost/Liters/Distance), and a monthly table (Month, Fuel €, Service €, Liters, Distance, Total €). Adds an **Export Filtered** action in the Stats tab.

**Changes**  
- `lib/pdf/active_vehicle_report.dart`: NotoSans font fallback; report builder using `seriesFromTotals`, `computeDistanceKm`, `estimateMonthlyDistanceKm`, `StatsFilter`, `StatsMetric`.  
- `lib/ui/tabs/stats_tab.dart`: **Export Filtered** wired to current filter & metric.  
- `test/pdf/pdf_stats_report_test.dart`: sanity tests (sample/empty/metric loop).  
- `CHANGELOG.md`: Day 12 entry.  
- Minor model field fixes (date/amount/odometerKm/totalAmount alignment).

**UX**  
- Export reflects on-screen filter & metric.  
- KPIs/table/chart numbers match the Stats screen (same data flow).  
- Zero-distance shows “—” consistently.  
- Full Unicode glyph coverage (€, arrows) via NotoSans.

**Quality**  
- `flutter analyze`: 0 issues  
- `flutter test`: 42+ passing  
- Build: PASS

**Risks**  
- Low. Additive changes, no API breaks.

**How to Test**  
1) Open **Stats** tab → select a filter & metric.  
2) Press **Export Filtered** → open the generated PDF.  
3) Verify: KPIs, chart values, monthly totals match the screen.

**Next**  
- Consider locale-aware typography review in the PDF.

**Update:** Added defensive font loading + global fontFallback to ensure full Unicode coverage in PDFs and avoid binding-related failures in tests.

**Update:** Fixed test OutsideTestException by moving a top-level expect into a proper test case; binding init now verified via smoke test.
